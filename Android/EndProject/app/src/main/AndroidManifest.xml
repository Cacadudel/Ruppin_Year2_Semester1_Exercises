<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EndProject"
        tools:targetApi="31">

        <!-- SplashActivity כעמוד פתיחה -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity כעמוד ראשי -->
        <activity android:name=".MainActivity" android:exported="true"/>

        <!-- GuessNumberActivity כעמוד לניחוש המספר -->
        <activity android:name=".GuessNumberActivity" android:exported="true"/>

        <!-- מסכים נוספים -->
        <activity android:name=".FillDetailsActivity" android:exported="true"/>
        <activity android:name=".MathCalculatorActivity" android:exported="true"/>
        <activity android:name=".NewGameActivity" android:exported="true"/>
    </application>

</manifest>
